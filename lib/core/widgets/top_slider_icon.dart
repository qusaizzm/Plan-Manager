import 'package:flutter/material.dart';

class TopSliderIcon extends StatelessWidget {
  const TopSliderIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.amber,
        height: 4,
        width: 30,
      ),
    );
  }
}
